@inherits LayoutComponentBase

<MudThemeProvider />
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>
    <MudAppBar>
        <MudText Typo="Typo.h6">Market Money</MudText>
        <MudSpacer/>
        <MudButton Variant="Variant.Text" Color="Color.Inherit" Href="/login">Вход</MudButton>
        <MudButton Variant="Variant.Text" Color="Color.Inherit" Href="/registration">Регистрация</MudButton>
    </MudAppBar>
    <MudMainContent>
        @Body
    </MudMainContent>
</MudLayout>

@code {
    bool _drawerOpen = true;

    void DrawerToggle()
    {
        _drawerOpen = !_drawerOpen;
    }
}